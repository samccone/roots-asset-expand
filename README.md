Roots Asset Expand - WIP
============

## Installing

`npm install roots-asset-expand --save`

```coffeescript
  module.exports =
    extensions: [assetExpand({lookup: "*.js"})]
```

> in your jade file

```jade
  // assets(*.js)
  h1 hi
```
